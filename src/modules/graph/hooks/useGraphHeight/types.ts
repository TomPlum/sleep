export interface GraphHeightProps {
  /**
   * The ID of the selected sleep
   * session.
   */
  selectedSession?: string
}

export interface GraphHeight {
  /**
   * The height of the graph
   * or graph placeholder.
   */
  height: string
}